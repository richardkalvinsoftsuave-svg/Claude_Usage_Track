# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Data validation & settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
pymysql>=1.1.0

# Templates & static files
jinja2>=3.1.0

# File uploads & images
python-multipart>=0.0.6
pillow>=10.2.0
aiofiles>=23.2.0

# HTTP client (for Ollama VLM fallback)
httpx>=0.26.0
requests>=2.31.0

# Environment config
python-dotenv>=1.0.0

# OCR (primary extraction path)
# PaddleOCR downloads its model weights on first use and caches them locally.
paddlepaddle>=2.6.0
paddleocr>=2.7.0

# Docling (alternative vision-based extraction)
# Docling downloads its model weights on first use and caches them locally.
docling>=2.0.0

# Testing
pytest>=8.0.0
